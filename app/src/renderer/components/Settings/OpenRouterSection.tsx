// OpenRouter Cloud-Backend (opt-in). Karte + Verhalten leben in CloudProviderSection —
// OpenRouter und LLMBase waren vorher zeilengleiche Kopien.
import { CloudProviderSection } from './CloudProviderSection'

export function OpenRouterSection() {
  return <CloudProviderSection provider="openrouter" />
}

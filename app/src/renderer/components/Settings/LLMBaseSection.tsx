// LLMBase Cloud-Backend (opt-in) — llmbase.ai, Eyloo GmbH. Karte + Verhalten leben in
// CloudProviderSection; der Regions-Vorbehalt (keine pauschale EU-Zusage) steht dort.
import { CloudProviderSection } from './CloudProviderSection'

export function LLMBaseSection() {
  return <CloudProviderSection provider="llmbase" />
}

{
  "name": "mindgraph-plugin-example",
  "version": "0.1.0",
  "private": true,
  "description": "Main-only Beispiel-/Starter-Plugin für MindGraph Notes (A0/3 Template).",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "keygen:dev": "node scripts/keygen-dev.mjs"
  },
  "devDependencies": {
    "@mindgraph/plugin-api": "^0.2.0",
    "esbuild": "^0.25.0"
  }
}
